# Флаги и валидация

## Общие правила валидации

### 1. Обработка ошибок
- Если не указаны необходимые флаги или они указаны некорректно:
  - Сначала выводим ошибку
  - Потом показываем полный help
  
### 2. Отсутствие флагов
- Если ни одного флага не указано:
  - Это не ошибка
  - Просто показываем help

### 3. Информационный режим (только -i)
- Если указан `-i`, но не указан `-o`:
  - Это не ошибка
  - Выводим информацию о всех коммитах (как сейчас)
  - Остальные флаги игнорируются (без -o они не имеют смысла)
  - Выводим уведомление: "Флаги будут проигнорированы, так как отсутствует -o"

---

## Tasks

### Core Changes
- [ ] Изменить логику валидации обязательных флагов
  - [ ] Если ошибки валидации → вывести ошибку + полный help
  - [ ] Если нет флагов вообще → просто help (без ошибки)
  - [ ] Если только -i без -o → режим информации (не ошибка)

- [ ] Реализовать режим "information mode" (только -i):
  - [ ] Вывод заголовка: "Информация о репозитории"
  - [ ] Вывод списка всех коммитов (как в текущем "Original commits:")
  - [ ] Вывод уведомления: "Флаги будут проигнорированы, так как отсутствует -o"
  - [ ] Завершение успешно без создания output репозитория

- [ ] Обновить парсинг аргументов:
  - [ ] При наличии -i без -o установить infoMode = true
  - [ ] В infoMode пропустить валидацию -o и остальных флагов

- [ ] Обновить main.go:
  - [ ] Если infoMode → вызвать новую функцию `printRepoInfo()`
  - [ ] Если нет ошибок и не infoMode → продолжить как сейчас

### Testing
- [ ] Добавить тест: только -i (без -o) → информация о репозитории
- [ ] Добавить тест: без флагов → help
- [ ] Добавить тест: некорректные флаги → ошибка + help
- [ ] Добавить тест: -i с -o → обычный режим

### Documentation
- [ ] Обновить README.md про режим только -i
- [ ] Добавить примеры для новых сценариев
